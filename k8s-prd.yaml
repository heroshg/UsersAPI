# UsersAPI — manifests do ambiente prd (namespace fcg-prd, criado pela plataforma — Orchestration).
# HA: 2+ réplicas, RDS privado na VPC (sem acesso público).
# Deploy: kubectl apply -f k8s-prd.yaml -n fcg-prd
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: users-api-sa
  namespace: fcg-prd
automountServiceAccountToken: false
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: users-api-config
  namespace: fcg-prd
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  Jwt__Issuer: "FiapCloudGames"
  Jwt__Audience: "FiapCloudGames"
  Jwt__ExpirationMinutes: "15"
  AWS__Region: "us-east-1"
  Messaging__Scope: "fcg-prd"
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: users-api-secrets
  namespace: fcg-prd
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: fcg-secretstore
    kind: ClusterSecretStore
  target:
    name: users-api-secrets
    creationPolicy: Owner
  data:
    - secretKey: Jwt__RsaPrivateKey
      remoteRef:
        key: fcg/prd/common/jwt
        property: Jwt__RsaPrivateKey
    - secretKey: Jwt__RsaPublicKey
      remoteRef:
        key: fcg/prd/common/jwt
        property: Jwt__RsaPublicKey
    - secretKey: ConnectionStrings__Users
      remoteRef:
        key: fcg/prd/users/db
        property: ConnectionStrings__Users
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: fcg-prd
  labels:
    app: users-api
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
    spec:
      serviceAccountName: users-api-sa
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1654
        runAsGroup: 1654
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: users-api
          image: 667079134782.dkr.ecr.us-east-1.amazonaws.com/users-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "750m"
          envFrom:
            - configMapRef:
                name: users-api-config
            - secretRef:
                name: users-api-secrets
            # ⚠️ Learner Lab: o SDK da AWS via IMDS (node role/LabRole) trava por causa do hop
            # limit de metadata dos nós (mesmo problema do ESO — ver Orchestration/k8s-dev.yaml).
            # Fallback: credenciais temporárias do lab como env vars. Secret populado
            # imperativamente, NUNCA commitado — recriar a cada sessão:
            # `kubectl create secret generic aws-lab-creds -n fcg-prd ...`.
            - secretRef:
                name: aws-lab-creds
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: fcg-prd
  labels:
    app: users-api
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
    service.beta.kubernetes.io/aws-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  selector:
    app: users-api
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: users-api
  namespace: fcg-prd
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: users-api
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-users-api-ingress
  namespace: fcg-prd
spec:
  podSelector:
    matchLabels:
      app: users-api
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - port: 8080
          protocol: TCP
