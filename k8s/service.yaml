apiVersion: v1
kind: Service
metadata:
  name: usersapi-service
  namespace: fiap-cloud-games
spec:
  type: NodePort
  selector:
    app: usersapi
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007